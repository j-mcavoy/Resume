image: dxjoke/tectonic-docker

stages:
    - build

build:
  stage: build
  script:
    - mkdir output
    - tectonic --outidr output cv.tex
    - tectonic --outidr output cover_letter.tex
  artifacts:
    paths:
      - output
